## HTTP Availability & Benchmark Tool

Консольная утилита для тестирования доступности и замера времени отклика HTTP-серверов. Программа позволяет отправлять множественные запросы к заданным хостам, собирать статистику и выводить подробный отчет о качестве соединения.

Проект выполнен в соответствии с техническим заданием, включая базовые требования, валидацию данных, работу с файлами и **реализацию конкурентного выполнения**.

### 🔀 Реализации (Ветки)

В репозитории представлены две архитектурные реализации решения задачи, разнесенные по веткам:

1.  **`main` (Asyncio)**:
    * Использует библиотеку `asyncio` и `aiohttp`.
    * Работает в одном потоке/процессе, используя событийный цикл (Event Loop).
    * Подойдет для стандартного использования (до 1000 хостов).
    * *Для использования:* `git checkout main`

2.  **`Multiprocessing` (Multiprocessing)**:
    * Использует библиотеку `multiprocessing` и `aiohttp`.
    * Запускает отдельные процессы для обработки пулов запросов, задействуя несколько ядер CPU.
    * Подойдет для высоконагруженного использования (от 1000 хостов).
    * *Для использования:* `git checkout Multiprocessing`

### ✨ Функциональность

* **Гибкий ввод хостов и условий:** Передача адреса (или нескольких через запятую) через аргументы командной строки или чтение из файла.
* **Настройка нагрузки:** Указание количества запросов (`count`) для каждого хоста для вычисления усредненной статистики.
* **Валидация:**
    * Проверка формата URL (валидный HTTP/HTTPS) с использованием регулярных выражений.
    * Проверка типов данных (числовые значения для count).
* **Обработка ошибок:** Human-readable вывод ошибок соединения, SSL, таймаутов и DNS.
* **Экспорт:** Возможность сохранения результатов в файл.
* **Статистика:** Детальный расчет метрик (Min, Max, Avg, Success, Failed, Errors).

### 🚀 Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/AlgoApi/infotecs-python
    cd infotecs-python
    ```

2.  **Выберите нужную реализацию:**
    ```bash
    # Для версии на Asyncio
    git checkout main

    # ИЛИ для версии на Multiprocessing
    git checkout Multiprocessing
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```
    для запуска тестов юнит тесты необходимо установить дополнительные зависимости не влияющие на работу утилиты:
    ```bash
    pip install -r requirements_tests.txt
    ```

## 🛠 Использование

Запуск осуществляется через файл `bench.py`.

### Аргументы командной строки

| Аргумент | Короткий | Тип | Описание |
| :--- | :--- | :--- | :--- |
| `--host` | `-H` | str | Хост(ы) для запроса. Можно указать несколько через запятую. (Взаимоисключающий с `-F`) |
| `--file` | `-F` | str | Путь к файлу со списком адресов (по одному на строку). (Взаимоисключающий с `-H`) |
| `--count` | `-C` | int | Кол-во запросов к каждому хосту (по умолчанию: 1). |
| `--timeout` | `-t` | int | Таймаут запроса в секундах (по умолчанию: 3). |
| `--action` | `-a` | str | HTTP метод: `get`, `post`, `put`, `patch`, `delete`, `options`. |
| `--output` | `-O` | str | Путь к файлу для сохранения вывода. |
| `--payload` | `-p` | str | JSON payload для тела запроса. |
| `--headers` | `-d` | str | Заголовки в формате JSON: `{"Key": "Value"}`. |
| `--cookies` | `-k` | str | Cookies в формате JSON: `{"Key": "Value"}`. |
| `--login` | `-n` | str | Логин для авторизации. |
| `--cli-pass` | `-l` | flag | Включить скрытый ввод пароля через консоль. (Игнорируется если указан `-i` или `-b`) |
| `--file-pass`| `-i` | str | Путь к файлу с паролем. (Требуются безопасные настройки доступа к файлу) (Игнорируется если указан `-l` или `-b`) |
| `--bearer` | `-b` | str | Bearer токен (Игнорирует пароли, если установлен). |
| `--log-level`| `-g` | str | Уровень логов: `debug`, `info`, `warn`, `err`, `fatal`, `success`. |
| `--verbose` | `-v` | flag | Выводить подробные данные об исключениях. |
| `--not-quiet`| `-q` | flag | Включить детальные логи (по умолчанию выключено). |
| `--http` | `-s` | flag | Использовать HTTP, если протокол не указан в адресе иначе будет использован HTTPS. |
| `--force` | `-r` | flag | Отключить проверку безопасности для файла с паролем. |

### 🧪 Примеры запуска

**1. Тест одного хоста по 5 запросов:**
```bash
python bench.py -H https://ya.ru -C 5
```
**2. Тест нескольких хостов по 10 запросов:**
```bash
python bench.py -H https://ya.ru,https://google.com -H https://career.infotecs.ru -C 10
```

**3. Чтение из файла и сохранение отчета в файл:**
```bash
python bench.py -F urls.txt -O report.txt
```

**4. Указание тела, заголовков и куки данных запроса:**
```bash
python bench.py -F urls.txt -O report.txt -p '{"somedata": 123}' -d '{"somedata": "123"}' -k '{"somedata": "123"}'
```

### 📌 Формат вывода статистики

Для каждого хоста формируется блок статистики со следующими показателями:
   * Host: Имя тестируемого хоста.
   * Success: Количество успешных запросов (HTTP 2xx, 3xx).
   * Failed: Количество запросов, на которые сервер ответил ошибкой (HTTP 4xx или 5xx).
   * Errors: Количество сетевых ошибок (таймауты, DNS, обрывы связи).
   * Min: Минимальное время ответа (сек).
   * Max: Максимальное время ответа (сек).
   * Avg: Среднее время выполнения запроса (сек).

Пример вывода:
```
{
    Host=ya.ru
    Success=1
    Failed=0
    Errors=0
    Min=1.0697
    Max=1.0697
    Avg=1.0697
}
```

## 🏗️ Внутреннее устройство утилиты

### 🧩 Multiprocessing

1. Точка входа:
    * Создаются объекты классов обработки и хранения аргументов командной строки `ArgumentParser`
    * Создаются объекты классов журналирования `Logger` и валидируем полученные данные `check_cli_arg`
    * Подсчёт необходимых распределительных групп, в целях отказоустойчивости (отказ одной группы не приведёт к падению утилиты)
2. Механизм создания рабочих групп:
    * Создание очереди пакетов
    * Чтение и группирование в пакеты хосты
    * Создание пула процессов с ограничением, основанное на доступных ресурсах машины
    * Чтение пакета из очереди
    * Создание единиц работы, обрабатывающие пакеты из очереди
    * Контролируем количество запущенных процессов
    * Журналируем ответ от единицы работы
3. Единица работы (в отдельном процессе):
    * Создаём локальный журнал
    * Создаются объекты классов подстановки параметров запросов и трассировки aiohttp
    * Создаётся сессия запросов
    * Создание задач запроса под своим namespace
    * Безопасное выполнение всех задач запроса и завершение единицы работы только при полном выполнении
4. Задача запроса (в процессе единицы работы):
    * Выполение задачи с ранее подставленными параметрами необходимое количество раз
    * Перехват исключений
    * Уже настроенная трассировка выполнит сбор данных
    * Возвращаем полученные данные



